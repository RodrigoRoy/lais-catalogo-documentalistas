export default defineAppConfig({
  ui: {
    colors: {
      // Quizá se requieran definir colores con mayor precisión
      // Revisar documentación en caso de ser necesario: https://ui.nuxt.com/getting-started/theme#tailwind-css
      primary: 'sky',
      secondary: 'red',
      neutral: 'zinc',
    }
  }
})
